{
  programs.wofi = {
    enable = true;
    settings = {
      height = "200px"; # Set your desired height (e.g., 200px, 30%, etc.)
      width = "300px"; # Optional: adjust width if needed
      location = "center"; # Optional: position the window
    };
  };
}
